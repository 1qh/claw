FROM ubuntu:24.04

RUN apt-get update && apt-get install -y \
    fuse3 \
    curl \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

RUN curl -fsSL https://install.tigerfs.io | HOME=/root sh

ENV PATH="/root/bin:$PATH"

ENTRYPOINT ["tigerfs"]
